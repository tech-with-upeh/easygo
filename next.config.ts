import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,  // Optional: Enables React Strict Mode
  allowedDevOrigins: ['http://172.20.10.4'], 
};

export default nextConfig;
